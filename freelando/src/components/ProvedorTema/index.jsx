import { ThemeProvider } from '@emotion/react'

const tema = {
    
}

export function ProvedorTheme({ children } ) {
    return (
        <ThemeProvider theme={tema}>

        </ThemeProvider>
    )
}